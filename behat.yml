default:
    extensions:
        Behat\MinkExtension:
            base_url: http://google.com
            goutte: ~
            selenium2: ~
    suites:
        default:
            contexts: [FeatureContext, BasketContext, WebContext]
